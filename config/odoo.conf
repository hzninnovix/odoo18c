[options]
admin_passwd = InnovixMaster125@#
addons_path = /mnt/extra-addons
proxy_mode = True

# Set memory limits to 4GB
limit_memory_hard = 4294967296
; 4 GB (Hard limit)
limit_memory_soft = 3758096384  ; 3.5 GB (Soft limit)

; Increased number of workers to use more memory effectively
workers = 10
; Increase workers to use more memory
limit_request = 8192
limit_time_cpu = 600
limit_time_real = 1200
max_cron_threads = 1

; Increase cache size to better utilize RAM
cache_size = 12288
; Set to 12 GB (12,288 MB) Cache size to use more memory

; Optionally, increase the max allowed memory usage per worker
;limit_memory_hard = 4294967296  ; This sets 4 GB as the hard memory usage per worker

; Additional memory tweaks for workers
;limit_request = 8192


# Increase memory limits for better RAM usage
;limit_memory_hard = 4294967296
; 4 GB (Hard limit)
;limit_memory_soft = 3758096384
; 3.5 GB (Soft limit)
;Increased number of workers to make better use of 4GB of RAM
;workers = 8
; Increase workers to use more memory effectively
;limit_request = 8192
;limit_time_cpu = 600
;limit_time_real = 1200
;max_cron_threads = 1
; Increase cache size to better utilize RAM
;cache_size = 8192
; 8 GB Cache (in MB, Odoo can use this much RAM for caching)
;ram-usage = 1.32GB



;limit_memory_hard = 2684354560
;limit_memory_soft = 2147483648
;limit_request = 8192
;limit_time_cpu = 600
;limit_time_real = 1200
;max_cron_threads = 1
;workers = 7
;ram-usage = 1.32GB

